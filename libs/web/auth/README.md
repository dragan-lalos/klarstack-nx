# Web Auth Library

React hooks and components for authentication in the Klastack-nx web application.

## Features

- `useLogin`: React Query mutation hook for login
- `useLogout`: Hook for logout functionality
- `RequireAuth`: Route guard component for protected routes

## Usage

```typescript
import { useLogin, useLogout, RequireAuth } from '@klastack-nx/web-auth';
```
