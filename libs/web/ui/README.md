# web-ui

Shared UI components for the web apps (design-system primitives + higher-level patterns).

## Patterns

For the “pattern” components (AppShell, PageHeader, etc.), see:

- `libs/web/ui/PATTERNS.md`
